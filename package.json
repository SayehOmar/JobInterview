{
  "name": "forest-bd-viewer",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.21",
    "turbo": "^1.11.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@apollo/server": "5.5.0",
    "@nestjs/config": "^4.0.4"
  },
  "overrides": {
    "@apollo/server": "5.5.0"
  }
}
