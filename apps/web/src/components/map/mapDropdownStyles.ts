/** Shared “card” dropdowns: profile, layers — match Explore area panel chrome. */
export const mapDropdownPanelClass =
    'w-56 rounded-xl border border-gray-200 bg-white shadow-xl animate-gear-open';

export const mapDropdownHeaderClass =
    'flex items-center gap-2 border-b border-gray-100 bg-gray-50 px-3 py-2';
